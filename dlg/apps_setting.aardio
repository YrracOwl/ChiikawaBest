import win.ui;
/*DSG{{*/
var winform = win.form(text="应用设置";right=447;bottom=207)
winform.add(
关闭时清空记录={cls="checkbox";text="关闭时清空记录";left=8;top=8;right=120;bottom=32;dl=1;dt=1;z=1}
)
/*}}*/

winform.bindConfig( ..cfg.apps_setting,{
	checkbox = {
		["^关闭时清空记录"] = "checked";
	};
	
} );

winform.show();
win.loopMessage();
return winform;
